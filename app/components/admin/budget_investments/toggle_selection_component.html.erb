<% if can?(:toggle_selection, investment) %>
  <%= button_to text, path, method: :patch, remote: true, class: html_class %>
<% else %>
  <%= t("admin.budget_investments.index.selected") if selected? %>
<% end %>
